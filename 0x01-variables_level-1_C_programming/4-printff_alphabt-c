#include <stdio.h>

/**
 * main - Prints "news" without 'q' and 'e'.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int i;

    for (i = 0; i < 4; i++)
    {
        char c;
        switch (i)
        {
            case 0:
                c = 'n';
                break;
            case 1:
                c = 'e';
                break;
            case 2:
                c = 'w';
                break;
            case 3:
                c = 's';
                break;
        }
        putchar(c);
    }
    putchar('\n');
    return 0;
}
