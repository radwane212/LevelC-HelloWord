#include <stdio.h>

/**
 * main - Prints "news all".
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char alp[] = "news all";
    int i;

    for (i = 0; i < sizeof(alp) - 1; i++)
    {
        putchar(alp[i]);
    }
    putchar('\n');
    return 0;
}
